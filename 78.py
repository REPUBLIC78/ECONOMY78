


# array_equations = ['x + y = 10 - z']
                    # 'TC = AFC * Q + AVC * Q'
                    # 'TC = TR - ((P - ATC) * Q)',
                    # 'TC = TR - Pr', 'ATC = AFC + AVC', 
                    # 'ATC = TC / Q', 'AFC = FC / Q', 
                    # 'AVC = VC / Q', 
                    # 'TR = P * Q',
                    # 'AR = TR / Q',
                    # 'MC = TC(Q) - TC(Q - 1)',
                    # 'MR = TR(Q) - TR(Q - 1)',
                    # 'Pr = TR - TC',
                    # 'Pr = (P - ATC) * Q',
                    # 'Qcr = FC/(P - AVC)' ]

# array_variables = [i for i in array_equations]

# for i in array_variables:
#     print(i)